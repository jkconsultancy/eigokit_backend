# ============================================================================
# Supabase Configuration
# ============================================================================
# Get these from your Supabase project dashboard: https://app.supabase.com/project/_/settings/api
# 
# SUPABASE_PROJECT_URL: Found in Project Settings > General > Reference ID
#   Format: https://your-project-ref.supabase.co
#
# SUPABASE_ANON_KEY: Found in Project Settings > API > Project API keys > anon public
#   This is the "Publishable key" in the Supabase dashboard
#
# SUPABASE_SERVICE_ROLE_KEY: Found in Project Settings > API > Project API keys > service_role
#   This is the "Secret key" in the Supabase dashboard
#   WARNING: Keep this secret! Never expose it in client-side code.
#
# SUPABASE_JWT_SECRET: Optional - JWT Signing Key for custom JWT verification
#   Usually not needed unless you're doing custom JWT verification
# ============================================================================
SUPABASE_PROJECT_URL=https://your-project-ref.supabase.co
SUPABASE_ANON_KEY=your_publishable_key_here
SUPABASE_SERVICE_ROLE_KEY=your_secret_key_here
SUPABASE_JWT_SECRET=optional_jwt_signing_key

# ============================================================================
# Email Service Configuration (Resend)
# ============================================================================
# Required for sending teacher invitation emails
# 
# Setup Instructions:
# 1. Sign up at https://resend.com (free tier: 3,000 emails/month)
# 2. Go to API Keys section and create a new API key
# 3. Copy the API key and paste it below
# 4. For development, you can use the test domain: onboarding@resend.dev
# 5. For production, verify your domain in Resend and use your verified email
#
# RESEND_API_KEY: Your Resend API key (starts with "re_")
#   Get it from: https://resend.com/api-keys
#
# RESEND_FROM_EMAIL: The email address that will appear as the sender
#   Development: Use "onboarding@resend.dev" (Resend's test domain - no verification needed)
#   Production: Use a verified domain email like "noreply@yourdomain.com"
#   To verify a domain: Go to Resend Dashboard > Domains > Add Domain
#
# RESEND_FORWARDING_URL: The base URL of your frontend application that will process the acceptance
#   This is used to generate invitation links in emails
#   Format: https://yourdomain.com (production) or http://localhost:5173 (development)
#   
#   IMPORTANT: 
#   - For local development: Use http://localhost:5173 (or your Vite dev server port)
#   - For production: Use your actual domain (e.g., https://app.eigokit.com)
#   - This URL should point to where your teacher app is hosted
#   - The invitation link will be: {APP_URL}/teacher/accept-invitation?token={token}
#   - Make sure this route exists in your teacher app frontend
#
#   Examples:
#   - Local: http://localhost:5173
#   - Vercel: https://eigokit-teacher.vercel.app
#   - Custom domain: https://teacher.eigokit.com
# ============================================================================
RESEND_API_KEY=re_your_api_key_here
RESEND_FROM_EMAIL=onboarding@resend.dev
RESEND_FORWARDING_URL=http://localhost:5173


# ============================================================================
# Password Reset
# ============================================================================
PASSWORD_RESET_REDIRECT_URL_PLATFORM_ADMIN=http://localhost:5173/auth/reset-password
PASSWORD_RESET_REDIRECT_URL_SCHOOL_ADMIN=http://localhost:5174/auth/reset-password
PASSWORD_RESET_REDIRECT_URL_TEACHER=http://localhost:5177/auth/reset-password


# ============================================================================
# Optional Configuration
# ============================================================================
# DATABASE_URL: Optional direct database connection URL (if not using Supabase client)
# ENVIRONMENT: Set to "production" for production deployments
# ============================================================================
# DATABASE_URL=postgresql://user:password@host:port/database
ENVIRONMENT=development
