# Supabase Configuration
# Get these values from your Supabase project dashboard: https://app.supabase.com/project/_/settings/api

# Project URL (found in Project Overview > Project API > Project URL)
SUPABASE_PROJECT_URL=https://your-project-ref.supabase.co

# Publishable Key (found in Project Overview > Project API > API Key (anon public)
# This is the anon/public key that can be safely used in client-side code# This is the anon/public key that can be safely used in client-side code
SUPABASE_ANON_KEY=your_publishable_key_here

# Secret Key (found in Project Settings > API keys - labeled as "secret key")
# This is the service role key with full database access
# WARNING: Keep this secret! Never expose this in client-side code.
SUPABASE_SERVICE_ROLE_KEY=your_secret_key_here

# JWT Signing Key (optional - found in Project Settings > API > JWT Settings)
# Only needed if you're doing custom JWT verification
# Can be ECC P-256 or HS256 format
SUPABASE_JWT_SECRET=your_jwt_signing_key_here

# Optional: Direct database connection URL (if needed)
# DATABASE_URL=postgresql://postgres:[password]@[host]:[port]/postgres

# Environment
ENVIRONMENT=development
