fastapi>=0.115.0
uvicorn[standard]>=0.32.0
supabase>=2.10.0
python-dotenv>=1.0.0
pydantic>=2.10.0
pydantic-settings>=2.6.0
python-jose[cryptography]>=3.3.0
passlib[bcrypt]>=1.7.4
python-multipart>=0.0.12

# Note: If using Python 3.13 and encountering build errors:
# Option 1: Install Rust: brew install rust (macOS) or https://rustup.rs/
# Option 2: Use Python 3.11 or 3.12 instead
# Option 3: Try: pip install --upgrade pip setuptools wheel && pip install -r requirements.txt

